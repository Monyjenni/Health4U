<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.health4u_v1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".SignupActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Login" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".activity_sidebar" />
        <activity android:name=".activity_setting" />
        <activity android:name=".workout_detail" />
        <activity android:name=".workout_page" />
        <activity android:name=".dashboard_screen" />
        <activity android:name=".workout_preview" />
        <activity android:name=".bmi_result" />
        <activity android:name=".DishDetail" />
        <activity android:name=".DishCookingStep" />
        <activity android:name=".DishPreview" />
        <activity android:name=".DishCategory" />

    </application>

</manifest>
